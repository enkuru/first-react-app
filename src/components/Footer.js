import React, {Component} from 'react';
import PropTypes from 'prop-types';

const Footer = () => {//stateless component örneği
  return (
    <div style={{marginTop: '20px'}}>footer</div>
  );
};

export default Footer;